# CODEX 追加仕様書
## Project: ESCAPE FROM THIRTIES
## Feature: SECRET MODE / 「みりんのハートにBダッシュ」
Version: 0.1 / 2026-08-13

---

## 0. この文書の位置づけ

この文書は、すでに動作している `ESCAPE FROM THIRTIES` の現行実装に、
**隠しモード（裏モード）を追加するための差分仕様書**である。

最優先事項：

- 現在成立している通常ゲームを壊さない。
- 既存コードを全面的に作り直さず、現行構造を読み取った上で最小限かつ保守しやすい変更を行う。
- 現在使用しているプレイヤーキャラクター画像・アニメーションは差し替えない。
- 通常モードのゲームバランスを勝手に変更しない。
- 仕様にない新機能を追加しない。

この追加仕様を実装する前に、必ず現行プロジェクト一式を確認し、
現在の画面遷移・ゲーム状態管理・config・アセット読込・BGM処理の方式を把握すること。

---

# 1. 目的

通常モードは、初見のユーザーでも遊びやすい生誕企画のランゲームとして維持する。

それとは別に、ゲームに慣れたユーザー向けの**高難度な隠しモード**を追加する。

隠しモードは最初からメニューに `HARD MODE` 等として表示しない。
通常タイトル画面に置かれた、やや不自然な **CONSOLE ボタン** から秘密のコマンドを入力することで起動する。

隠しモード名称：

**みりんのハートにBダッシュ**

これは通常モードとは別タイトルとして扱う。

---

# 2. 通常タイトル画面への追加

現在の通常タイトル画面に、既存の `START` / `STORY` を妨げない形で、
小さめの **CONSOLE** ボタンを追加する。

要件：

- `START` より目立たせない。
- `STORY` とも別ボタンとして存在する。
- 「隠しモード」「裏技」「HARD」など、機能を直接説明する文言は表示しない。
- わざと少し怪しい、押してみたくなる程度の存在感にする。
- スマホ縦画面で誤タップしにくい位置に置く。

MVP段階では装飾は簡易でよい。

---

# 3. CONSOLE 画面

`CONSOLE` を押すと、ゲーム内の簡易コンソール画面を開く。

表示するもの：

- ゲームボーイ等の携帯ゲーム機を連想する簡素な操作盤
- 十字キー：UP / DOWN / LEFT / RIGHT
- A ボタン
- B ボタン
- BACK または CLOSE

見た目は最終デザインではないため、現行UIに馴染む仮デザインでよい。

## 3.1 入力コマンド

以下の順番を正しく入力した場合のみ成功とする。

`UP → UP → DOWN → DOWN → A → B → A → B`

表示上は記号を用いてもよい。

`↑ ↑ ↓ ↓ A B A B`

## 3.2 入力判定

- ボタンを押すたびに入力履歴を内部で保持する。
- 正解シーケンスと一致したら解除処理へ進む。
- 間違えた場合、ユーザーへ長いエラー説明は不要。
- 実装は単純でよい。正解コマンドの長さを超えて履歴を無制限に保持しない。
- キーボード操作を追加してもよいが、スマホタッチ操作を必須とする。

## 3.3 成功演出

コマンド成功時、短い演出を挟んで以下を表示する。

**みりんのハートにBダッシュ**

その後、隠しモード専用タイトル画面へ遷移する。

演出は軽量でよい。
画面フラッシュ、短いノイズ、文字表示など、既存実装を壊さない簡単な方法を選ぶ。

過剰なアニメーションや新規画像生成は不要。

---

# 4. 隠しモード専用タイトル

通常タイトルとは別に、隠しモード用タイトル状態を作る。

必須表示：

- `みりんのハートにBダッシュ`
- START
- NORMAL MODE（通常タイトルへ戻る）

必要であれば HIGH SCORE 表示を置いてもよいが、現行ゲームにハイスコア機能が存在する場合のみ、その構造を流用すること。

## 4.1 NORMAL MODE

`NORMAL MODE` を押すと通常タイトル画面へ戻る。

通常タイトルへ戻った後、再び隠しモードへ入る場合は CONSOLE からコマンドを入力する。

**永続アンロックは必須ではない。**
X等のアプリ内ブラウザから遊ばれることを想定し、localStorage等の永続性にゲーム進行を依存させない。

---

# 5. 隠しモードのゲーム仕様

隠しモードは通常ゲームと同じ基本システムを使用する。

変更する主な要素は、**速度・ライフ・難度**である。

## 5.1 プレイヤー

- 現在使用中の通常モードと同じプレイヤーキャラクターを使用する。
- キャラクター画像を差し替えない。
- run / jump / damage 等の既存アニメーションをそのまま利用する。
- プレイヤーの当たり判定方式も原則として既存実装を流用する。

## 5.2 LIFE

隠しモード開始時：

**LIFE = 1**

障害物に一度衝突した時点で GAME OVER とする。

既存の衝突・無敵時間処理を流用して構わないが、
LIFE 1 のため一回の有効な被弾でゲーム終了すること。

## 5.3 速度

通常モードより明確に速い基本スクロール速度を使用する。

ただし、数値はハードコードせず調整可能にする。

例：

```js
SECRET_BASE_SPEED
SECRET_BOOST_SPEED
```

あるいは既存config構造に合わせて、mode別設定としてまとめる。

具体値はCodexが現行の `BASE_SPEED` 等を確認し、まずプレイ可能な仮値を設定する。
最終値はユーザーが実機試遊して調整する。

## 5.4 BOOST

既存の BOOST システムは**隠しモードでも使用可能**とする。

- 通常時点ですでに高速
- BOOSTするとさらに高速
- BOOSTによる既存のスコア倍率・効果はできるだけ流用

ただし、隠しモードの速度を上げた結果、ジャンプで物理的に突破不可能な配置を作らないこと。

BOOST中を含め、ジャンプ・障害物サイズ・配置の組み合わせが理論上クリア可能か確認する。

---

# 6. 隠しモードのコース難度

目的：

**完走そのものが難しい。何度も再挑戦したくなる。**

通常モードより明確に高難度とする。

ただし、不可能配置や完全な初見殺しだけで難しくしない。

推奨：

- 通常より速い速度に合わせた障害物間隔
- ジャンプタイミングをより厳しくする
- LIFE 1 によりミスを許さない
- 後半ほどプレッシャーが上がる固定配置
- 覚えて上達できる固定コース

現行通常コースをベースに調整しても、隠しモード専用コースデータを用意してもよい。

**実装上は、通常コースと隠しコースをデータで分離できる構造を推奨する。**

例：

```js
NORMAL_COURSE
SECRET_COURSE
```

ただし既存実装に合わせ、不要な大規模リファクタリングはしない。

---

# 7. ジャンプ成立性

隠しモードではワールド速度を上げるため、既存の固定ジャンプ軌道との組み合わせに注意する。

必ず確認すること：

- 隠しモード基本速度で、全必須障害物をジャンプ可能
- BOOST中にも、コース設計上必要なジャンプが成立する
- 障害物の横幅に対して滞空時間が不足していない
- 高速化によって当たり判定のすり抜けが発生しない

必要なら隠しモード専用のジャンプ値を持たせてもよい。

例：

```js
SECRET_GRAVITY
SECRET_JUMP_VELOCITY
```

ただし、通常モードの操作感は変更しない。

---

# 8. GAME OVER / CLEAR 後の遷移

ここは重要。

隠しモードでプレイ中の場合：

### GAME OVER

- GAME OVER表示
- SCORE等、既存Result情報を必要に応じて表示
- RETRY
- SECRET TITLE
- NORMAL MODE は隠しタイトル側に存在すればよく、Resultへ重複配置しなくてもよい

### CLEAR

- 隠しモード専用のCLEAR状態として扱う
- SCORE等、既存Result情報を表示してよい
- RETRY
- SECRET TITLE

**GAME OVER / CLEAR 後に通常タイトルへ自動で戻さない。**

ユーザーが隠しモードを連続で再挑戦できることを優先する。

`RETRY` は可能なら直接隠しモードを再スタートし、待ち時間を短くする。

---

# 9. スコア / ハイスコア

既存スコアシステムを原則流用する。

隠しモードは速度・ライフ条件が異なるため、通常モードとスコア条件が同一とはみなさない。

現行実装にハイスコア保存がある場合：

- 通常モードのHIGH SCORE
- 隠しモードのHIGH SCORE

を別キーで管理できるようにすることを推奨する。

例：

```js
highScoreNormal
highScoreSecret
```

ただし、ハイスコア保存そのものが現行で未実装なら、今回の追加仕様のためだけに必須実装とはしない。

---

# 10. 背景

**現段階では通常モードと同じ背景を使用してよい。**

ただし将来、隠しモード専用背景へ簡単に差し替えられるようにする。

要件：

- mode別に背景アセットを指定可能
- secret用背景が未設定ならnormal背景へfallback
- 背景変更のためにゲームロジックを編集する必要がない構造

例：

```js
background: {
  normal: 'assets/background/normal.png',
  secret: 'assets/background/secret.png'
}
```

ファイル名・構造は現行コードに合わせてよい。

**Codexは新しい本番背景を生成しない。**

---

# 11. BGM

**現段階では通常モードと同じBGMを使用してよい。**

将来、隠しモード専用BGMへ変更する可能性がある。

そのため：

- mode別にBGMを設定可能にする
- secret BGM未設定時はnormal BGMへfallbackしてよい
- BGMアセットの差し替えがゲームロジック変更なしで可能な形が望ましい

例：

```js
bgm: {
  normal: 'assets/audio/normal.mp3',
  secret: 'assets/audio/secret.mp3'
}
```

既存の音声管理方式がある場合はそれに従う。

**新規BGMを自動生成しない。**

---

# 12. STORY テキスト

通常タイトルの `STORY` は現在の仕様を維持する。

将来、本文を変更する予定があるため、既存仕様どおりテキストを差し替えやすい構造を維持する。

隠しモード側について：

- 現段階で専用STORYボタンは必須ではない。
- もし既存UI構造上簡単に共用できる場合でも、勝手に裏モード専用長文を作成しない。
- STORY本文をコード内の複数箇所へ重複して埋め込まない。

本番文章はユーザーが後から決定する。

---

# 13. モード管理

通常 / 隠しモードの差分を、ゲームコード内で大量の `if` として散在させないこと。

現行構造に合わせて、最低限以下の状態を区別できるようにする。

例：

```js
mode = 'normal'
mode = 'secret'
```

または：

```js
GAME_MODES.normal
GAME_MODES.secret
```

modeごとに少なくとも以下を切り替えやすくする。

- LIFE
- base speed
- boost speed / multiplier（必要なら）
- jump関連値（必要なら）
- course data
- background
- BGM
- high score key（既存時のみ）
- Result後の遷移先

通常モードの既存設定値はできるだけ変更せず、その上にsecret設定を追加する。

---

# 14. スマホ / Xアプリ内ブラウザ前提

主な入口は、X等のSNSアプリ内ブラウザからURLを開くケースを想定する。

そのため：

- CONSOLEの十字キー / A / B はtouchで確実に反応する
- 小さすぎるボタンにしない
- ページスクロールと入力が競合しない
- 長押しや二本指操作を要求しない
- 秘密モードの成立にlocalStorageの永続性を必須としない
- 画面リロード後に秘密モードが解除されても仕様上問題ない

現在のページを開いている間、隠しタイトル → プレイ → GAME OVER → RETRY のループが安定して動くことを優先する。

---

# 15. 今回やらないこと

この追加仕様では以下を実装しない。

- 新しいプレイヤーキャラクター制作 / 差し替え
- オンラインランキング
- アカウント
- サーバー処理
- 永続的な裏モード解放を必須化
- 新しいゲームシステム
- ボス戦
- 複数の隠しコマンド
- コマンド一覧画面
- 実績システム
- 新規本番背景の自動生成
- 新規BGMの自動生成
- STORY本文の創作
- 通常モードの難度変更

目的はあくまで、**現在成立しているゲームへ、高難度の裏モードを一つ追加すること**。

---

# 16. Definition of Done

以下を満たしたら、隠しモード追加のMVP完成とする。

- [ ] 通常タイトルに CONSOLE ボタンがある
- [ ] CONSOLEをスマホtouchで操作できる
- [ ] `↑ ↑ ↓ ↓ A B A B` を正しく入力すると解除される
- [ ] 成功時に「みりんのハートにBダッシュ」と表示される
- [ ] 隠しモード専用タイトルへ遷移する
- [ ] 隠しタイトルから通常タイトルへ戻れる
- [ ] 隠しモードは現在のプレイヤーキャラをそのまま使用する
- [ ] LIFE = 1 で開始する
- [ ] 通常モードより明確に高速である
- [ ] BOOSTを使用できる
- [ ] 全コースが理論上クリア可能である
- [ ] GAME OVER後、通常タイトルではなく隠しモード側へ戻れる
- [ ] RETRYで隠しモードを連続再挑戦できる
- [ ] CLEAR後も隠しモード側のResult / Titleへ戻る
- [ ] 通常ゲームの挙動が変更・破損していない
- [ ] 背景をnormal / secretで後から分けられる
- [ ] BGMをnormal / secretで後から分けられる
- [ ] STORY本文が後から差し替えやすい状態を維持する
- [ ] GitHub Pages上で通常モード・隠しモード双方が動作する
- [ ] iPhone等の実機ブラウザでCONSOLE入力とゲーム操作を確認できる

---

# 17. 実装後にCodexが報告すること

実装完了後、以下を簡潔に報告する。

1. 変更したファイル一覧
2. CONSOLE / 隠しコマンド処理を実装したファイル
3. 隠しモード設定値がある場所
4. 隠しコースを編集する場所
5. 隠しモード速度を変更する場所
6. 隠しモード用背景の差し替え方法
7. 隠しモード用BGMの差し替え方法
8. STORY本文を変更する場所
9. 通常 / 隠し HIGH SCORE の扱い
10. スマホ実機で確認すべき項目
11. GitHub Pages反映前に確認すべき点

また、実装後に通常モードを一通りプレイし、
**追加機能による既存挙動の回帰がないことを確認すること。**

---

# 18. Codexへの依頼文

このファイルを同じプロジェクト内に置いたうえで、Codexへ以下を依頼する。

> `CODEX_SECRET_MODE_ADDENDUM.md` を読んでください。  
> 現在のゲーム実装を先に確認し、既存の通常モードを壊さない形で、この追加仕様に従って隠しモードを実装してください。  
> 大規模な書き直しは避け、現在のコード構造を活かしてください。  
> プレイヤーキャラクターは現在のものをそのまま使用してください。  
> 背景・BGM・STORY本文は後からユーザーが差し替えるため、差し替えやすい構造だけ用意し、本番素材や本文を勝手に作らないでください。  
> 実装後はこの仕様書17章の内容を報告し、通常モードに回帰がないか確認してください。

